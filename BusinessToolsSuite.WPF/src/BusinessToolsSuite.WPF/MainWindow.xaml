<Window x:Class="BusinessToolsSuite.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BusinessToolsSuite.WPF.ViewModels"
        xmlns:converters="clr-namespace:BusinessToolsSuite.WPF.Converters"
        xmlns:behaviors="clr-namespace:BusinessToolsSuite.WPF.Behaviors"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Title="{Binding Title}"
        Width="1200" Height="800"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="D1" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchExpireWiseCommand}"/>
        <KeyBinding Key="D2" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchAllocationBuddyCommand}"/>
        <KeyBinding Key="D3" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchEssentialsBuddyCommand}"/>
    </Window.InputBindings>

    <Window.Resources>
        <!-- Converters -->
        <converters:ViewModelToViewConverter x:Key="ViewModelToView"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <!-- Card Button Style with Hover Effects -->
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="330"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="15"
                                                  ShadowDepth="5"
                                                  Opacity="0.2"
                                                  Color="#000000"/>
                            </Border.Effect>
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.RenderTransformOrigin>
                                <Point X="0.5" Y="0.5"/>
                            </Border.RenderTransformOrigin>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FAFAFA"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="1.05" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="1.05" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="1.0" Duration="0:0:0.2">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="0.98" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="0.98" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             To="1.0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             To="1.0" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Theme Toggle Button Style -->
        <Style x:Key="ThemeToggleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="16,10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Modern Gradient Background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#667eea" Offset="0.0"/>
                <GradientStop Color="#764ba2" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Launcher View (shown when no module is active) -->
        <Grid Visibility="{Binding NavigationService.CurrentView, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with improved spacing -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,50,0,0">
            <TextBlock Text="⚙️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
            <TextBlock Text="Business Tools Suite"
                       FontSize="44"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,6"/>
            <TextBlock Text="Professional Business Management Tools"
                       FontSize="18"
                       Foreground="#E8E8E8"
                       HorizontalAlignment="Center"
                       FontWeight="Light"
                       Margin="0,0,0,6"/>
            <Border Background="#40FFFFFF"
                    CornerRadius="20"
                    Padding="14,7"
                    HorizontalAlignment="Center">
                <TextBlock Text="Select an application to begin →"
                           FontSize="15"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- Module Cards with enhanced design -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0,20,0,20">

            <!-- ExpireWise Card -->
            <Grid Margin="0,0,16,0">
                <Button Command="{Binding LauncherViewModel.LaunchExpireWiseCommand}"
                        Style="{StaticResource CardButtonStyle}">
                    <StackPanel Margin="28">
                        <Border Background="#FEF3C7"
                                CornerRadius="16"
                                Width="72"
                                Height="72"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,16">
                            <TextBlock Text="📦"
                                       FontSize="42"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="ExpireWise"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="#1e293b"
                                   Margin="0,0,0,14"/>
                        <TextBlock Text="Track product expiration dates and manage inventory lifecycle with automated alerts"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Foreground="#64748b"
                                   FontSize="13"
                                   LineHeight="20"
                                   Margin="0,0,0,16"/>
                        <Border Background="#FEF3C7"
                                CornerRadius="8"
                                Padding="12,6"
                                HorizontalAlignment="Center">
                            <TextBlock Text="Alt + 1"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#92400e"/>
                        </Border>
                    </StackPanel>
                </Button>

                <!-- Pop-out button overlay -->
                <Button Command="{Binding LauncherViewModel.PopOutExpireWiseCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="16"
                        Width="36"
                        Height="36"
                        Background="#FEF3C7"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="Open in separate window">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Width="36"
                                    Height="36">
                                <TextBlock Text="🗗"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#FDE68A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Allocation Buddy Card -->
            <Grid Margin="16,0,16,0">
                <Button Command="{Binding LauncherViewModel.LaunchAllocationBuddyCommand}"
                        Style="{StaticResource CardButtonStyle}">
                    <StackPanel Margin="28">
                        <Border Background="#DDD6FE"
                                CornerRadius="16"
                                Width="72"
                                Height="72"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,16">
                            <TextBlock Text="📊"
                                       FontSize="42"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Allocation Buddy"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="#1e293b"
                                   Margin="0,0,0,14"/>
                        <TextBlock Text="Manage store allocations with smart categorization and real-time inventory distribution"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Foreground="#64748b"
                                   FontSize="13"
                                   LineHeight="20"
                                   Margin="0,0,0,16"/>
                        <Border Background="#DDD6FE"
                                CornerRadius="8"
                                Padding="12,6"
                                HorizontalAlignment="Center">
                            <TextBlock Text="Alt + 2"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#5b21b6"/>
                        </Border>
                    </StackPanel>
                </Button>

                <!-- Pop-out button overlay -->
                <Button Command="{Binding LauncherViewModel.PopOutAllocationBuddyCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="16"
                        Width="36"
                        Height="36"
                        Background="#DDD6FE"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="Open in separate window">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Width="36"
                                    Height="36">
                                <TextBlock Text="🗗"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#C4B5FD"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Essentials Buddy Card -->
            <Grid Margin="16,0,0,0">
                <Button Command="{Binding LauncherViewModel.LaunchEssentialsBuddyCommand}"
                        Style="{StaticResource CardButtonStyle}">
                    <StackPanel Margin="28">
                        <Border Background="#D1FAE5"
                                CornerRadius="16"
                                Width="72"
                                Height="72"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,16">
                            <TextBlock Text="📋"
                                       FontSize="42"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Essentials Buddy"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="#1e293b"
                                   Margin="0,0,0,14"/>
                        <TextBlock Text="Monitor Business Central bin contents and track essential inventory with comprehensive reporting"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Foreground="#64748b"
                                   FontSize="13"
                                   LineHeight="20"
                                   Margin="0,0,0,16"/>
                        <Border Background="#D1FAE5"
                                CornerRadius="8"
                                Padding="12,6"
                                HorizontalAlignment="Center">
                            <TextBlock Text="Alt + 3"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#065f46"/>
                        </Border>
                    </StackPanel>
                </Button>

                <!-- Pop-out button overlay -->
                <Button Command="{Binding LauncherViewModel.PopOutEssentialsBuddyCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="16"
                        Width="36"
                        Height="36"
                        Background="#D1FAE5"
                        BorderThickness="0"
                        Cursor="Hand"
                        ToolTip="Open in separate window">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Width="36"
                                    Height="36">
                                <TextBlock Text="🗗"
                                           FontSize="18"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#A7F3D0"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

        </StackPanel>

        <!-- Footer with improved design -->
        <Grid Grid.Row="2" Margin="0,0,0,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left spacer -->
            <Border Grid.Column="0"/>

            <!-- Center text -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="v1.0.0"
                           Foreground="#E8E8E8"
                           FontSize="13"
                           HorizontalAlignment="Center"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <TextBlock Text="All tools run 100% offline • Built with .NET 8 &amp; WPF"
                           Foreground="#D0D0D0"
                           FontSize="12"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Right side - Settings and Theme toggle -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,0,48,0">
                <Button Command="{Binding OpenSettingsCommand}"
                        Style="{StaticResource ThemeToggleButtonStyle}"
                        Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙️" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Settings" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding LauncherViewModel.ToggleThemeCommand}"
                        Style="{StaticResource ThemeToggleButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="MoonIcon" Text="🌙" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock x:Name="SunIcon" Text="☀️" FontSize="16" Margin="0,0,10,0" Visibility="Collapsed"/>
                        <TextBlock Text="Theme" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
        </Grid>

        <!-- Module View (shown when a module is active) -->
        <Grid Visibility="{Binding NavigationService.CurrentView, Converter={StaticResource NullToVisibility}}">
            <Grid.Background>
                <SolidColorBrush Color="#F5F5F5"/>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Navigation Bar with Back Button -->
            <Border Grid.Row="0"
                    Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,0,1"
                    Padding="16,12">
                <Grid>
                    <Button HorizontalAlignment="Left"
                            Command="{Binding NavigationService.NavigateBackCommand}"
                            Cursor="Hand"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="12,8">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="←" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Back to Launcher" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#E8E8E8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Module Content Area with Transitions -->
            <ContentControl Grid.Row="1"
                            Content="{Binding NavigationService.CurrentView, Converter={StaticResource ViewModelToView}}"
                            behaviors:ContentTransitionBehavior.EnableTransition="True"/>
        </Grid>
    </Grid>

</Window>
