<UserControl x:Class="BusinessToolsSuite.WPF.Views.AllocationBuddy.SelectLocationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BusinessToolsSuite.WPF.ViewModels"
             mc:Ignorable="d"
             Width="400" Height="200"
             d:DataContext="{d:DesignInstance Type=vm:SelectLocationDialogViewModel}">

    <StackPanel Margin="12">
        <TextBlock Text="Select a target location to move the item to:" Margin="0,0,0,12"/>
        <ComboBox ItemsSource="{Binding Locations}" SelectedItem="{Binding SelectedLocation}" Margin="0,0,0,12"/>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
            <TextBlock Text="Quantity:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="90" Text="{Binding SelectedQuantity, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelBtn" Content="Cancel" Click="Cancel_Click" Width="80" Margin="0,0,8,0"/>
            <Button x:Name="OkBtn" Content="OK" Click="Ok_Click" Width="80"/>
        </StackPanel>
    </StackPanel>
</UserControl>
