<UserControl x:Class="BusinessToolsSuite.WPF.Views.EssentialsBuddy.EssentialsBuddyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BusinessToolsSuite.WPF.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:EssentialsBuddyViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left side buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Command="{Binding ImportFromExcelCommand}" Padding="12,6" Margin="0,0,8,0"
                            Background="#007ACC" Foreground="White">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Import Excel" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding ImportFromCsvCommand}" Padding="12,6" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Import CSV" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Separator Width="1" Margin="8,0"/>

                    <Button Command="{Binding EditItemCommand}" Padding="12,6" Margin="8,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Edit" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding DeleteItemCommand}" Padding="12,6" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Delete" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Separator Width="1" Margin="8,0"/>

                    <Button Command="{Binding LoadItemsCommand}" Padding="12,6" Margin="8,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Refresh" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Right side buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding ExportToExcelCommand}" Padding="12,6" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Export Excel" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding ExportToCsvCommand}" Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Export CSV" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search and Filter Bar -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="8" VerticalAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Search by item number, description, bin code, or category..."
                                                             Foreground="Gray" Margin="8,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Status Filter -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Status:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding StatusFilter}"
                              Width="150" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Total Value Display -->
                <Border Grid.Column="2" Background="#4CAF50" Padding="12,6" Margin="16,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding TotalInventoryValue, StringFormat='Total Value: {0:C2}'}"
                                   Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" Margin="6,0,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Item Count -->
                <TextBlock Grid.Column="3" Text="{Binding FilteredItems.Count, StringFormat='Items: {0}'}"
                           VerticalAlignment="Center" Margin="16,0,0,0" FontWeight="SemiBold" Foreground="#666"/>
            </Grid>
        </Border>

        <!-- Welcome Screen (shown when no data) -->
        <Border Grid.Row="2" Background="White" Visibility="{Binding HasNoData, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Welcome to Essentials Buddy" FontSize="28" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock Text="Import your inventory data to track essentials (9-90* bins)" FontSize="16" Foreground="#666"
                           HorizontalAlignment="Center" Margin="0,0,0,24"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding ImportFromExcelCommand}" Background="#007ACC" Foreground="White"
                            Padding="24,12" Margin="0,0,16,0" FontSize="16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Import Excel File" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ImportFromCsvCommand}" Padding="24,12" FontSize="16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Import CSV File" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <TextBlock Text="Only items from 9-90* bins will be imported and aggregated" FontSize="12" Foreground="#666"
                           HorizontalAlignment="Center" Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <!-- Data Grid (shown when has data) -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredItems}" SelectedItem="{Binding SelectedItem}"
                  IsReadOnly="True" AutoGenerateColumns="False" GridLinesVisibility="All"
                  Background="White" CanUserReorderColumns="True" CanUserResizeColumns="True"
                  Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityConverter}}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Number" Binding="{Binding ItemNumber}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Bin Code" Binding="{Binding BinCode}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding QuantityOnHand}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Min" Binding="{Binding MinimumThreshold}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Max" Binding="{Binding MaximumThreshold}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat='{}{0:C2}'}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat='{}{0:C2}'}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Last Updated" Binding="{Binding LastUpdated, StringFormat='{}{0:MMM dd, yyyy}'}" Width="140" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#666"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Loading..." VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</UserControl>
