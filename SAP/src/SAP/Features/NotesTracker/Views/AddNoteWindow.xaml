<Window x:Class="SAP.Features.NotesTracker.Views.AddNoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Note" Height="320" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Vendor" Grid.Row="0"/>
        <TextBox x:Name="VendorBox" Grid.Row="0" Margin="0,22,0,0"/>

        <TextBlock Text="Transfers" Grid.Row="1" Margin="0,48,0,0"/>
        <TextBox x:Name="TransfersBox" Grid.Row="1" Margin="0,70,0,0"/>

        <TextBlock Text="WH Shipments" Grid.Row="2" Margin="0,96,0,0"/>
        <TextBox x:Name="WhsBox" Grid.Row="2" Margin="0,118,0,0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,150,0,0">
            <Button x:Name="PickColorBtn" Content="Pick Color" Click="PickColorBtn_Click"/>
            <Border x:Name="ColorPreview" Width="24" Height="24" Margin="8,0,0,0" Background="#B56576" CornerRadius="3"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Add" Width="80" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
