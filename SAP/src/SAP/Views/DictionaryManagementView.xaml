<UserControl x:Class="SAP.Views.DictionaryManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SAP.ViewModels"
             xmlns:converters="clr-namespace:SAP.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:DictionaryManagementViewModel}">

    <UserControl.Resources>
        <converters:ListToCommaStringConverter x:Key="ListToCommaStringConverter"/>
        
        <!-- Item Template for Items ListView -->
        <DataTemplate x:Key="ItemTemplate">
            <Border BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="0,0,0,1" Padding="8,6" Background="{DynamicResource SurfaceBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding Number}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Description}" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="10,0"/>
                    <TextBlock Grid.Column="2" Text="{Binding Skus, Converter={StaticResource ListToCommaStringConverter}}" 
                               Foreground="{DynamicResource TextTertiaryBrush}" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                </Grid>
            </Border>
        </DataTemplate>
        
        <!-- Item Template for Stores ListView -->
        <DataTemplate x:Key="StoreTemplate">
            <Border BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="0,0,0,1" Padding="8,6" Background="{DynamicResource SurfaceBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding Code}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="10,0"/>
                    <Border Grid.Column="2" Background="#022c22" CornerRadius="3" Padding="6,2" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Rank}" Foreground="#34d399" FontWeight="SemiBold" FontSize="11"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="{DynamicResource BackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,10,0,0">
            
            <!-- Header Card -->
            <Border Background="{DynamicResource SurfaceBrush}" Padding="20" CornerRadius="8" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Dictionary Management" FontSize="18" FontWeight="SemiBold" Foreground="#8b5cf6" Margin="0,0,0,8"/>
                        <TextBlock Text="Manage item and store dictionaries used for import accuracy across AllocationBuddy, ExpireWise, and EssentialsBuddy."
                                   FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#a78bfa" FontWeight="Medium" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" Content="💾 Save All Changes" Command="{Binding SaveDictionaryCommand}"
                            Background="#10b981" Foreground="White" BorderThickness="0" 
                            Padding="20,12" FontSize="13" FontWeight="SemiBold" Cursor="Hand" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Items and Stores Side-by-Side -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Items Section -->
                <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="0">
                    <StackPanel>
                        <!-- Items Header -->
                        <Border Background="#6366f1" CornerRadius="8,8,0,0" Padding="15,12">
                            <TextBlock Text="Items" FontSize="15" FontWeight="SemiBold" Foreground="White"/>
                        </Border>
                        
                        <StackPanel Margin="15">
                            <!-- Search -->
                            <TextBox Text="{Binding ItemSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Padding="10,8" FontSize="13" Background="{DynamicResource SurfaceHoverBrush}" Foreground="{DynamicResource TextPrimaryBrush}" 
                                     BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="1" Margin="0,0,0,12"/>

                            <!-- Add/Edit Form -->
                            <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="Add/Edit Item" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                                    
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="2*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Item Number" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding NewItemNumber, UpdateSourceTrigger=PropertyChanged}" 
                                                     Padding="8,6" FontSize="12" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Description" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding NewItemDescription, UpdateSourceTrigger=PropertyChanged}" 
                                                     Padding="8,6" FontSize="12" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <TextBlock Text="SKUs (comma-separated)" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,10,0,4"/>
                                    <TextBox Text="{Binding NewItemSkus, UpdateSourceTrigger=PropertyChanged}" 
                                             Padding="8,6" FontSize="12" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                        <Button Content="Add New" Command="{Binding AddItemCommand}"
                                                Background="#10b981" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,0,8,0"/>
                                        <Button Content="Update Selected" Command="{Binding UpdateItemCommand}"
                                                Background="#6366f1" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,0,8,0"/>
                                        <Button Content="Delete Selected" Command="{Binding DeleteItemCommand}"
                                                Background="#ef4444" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,0,8,0"/>
                                        <Button Content="Clear Form" Command="{Binding ClearItemFormCommand}"
                                                Background="{DynamicResource TextDisabledBrush}" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand"/>
                                    </StackPanel>
                                    <Button Content="📥 Import from Excel" Command="{Binding ImportItemsFromExcelCommand}"
                                            Background="#f59e0b" Foreground="White" BorderThickness="0" 
                                            Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,10,0,0" HorizontalAlignment="Left"
                                            ToolTip="Import items from Excel file (Number, Description, SKUs columns)"/>
                                </StackPanel>
                            </Border>

                            <!-- Items List Header -->
                            <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="4,4,0,0" Padding="8,6" BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="1,1,1,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="150"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Number" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Grid.Column="1" Text="Description" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="10,0"/>
                                    <TextBlock Grid.Column="2" Text="SKUs" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Items List -->
                            <Border BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="1" CornerRadius="0,0,4,4">
                                <ListView ItemsSource="{Binding FilteredItems}"
                                          SelectedItem="{Binding SelectedItem}"
                                          ItemTemplate="{StaticResource ItemTemplate}"
                                          Height="250"
                                          BorderThickness="0"
                                          Background="{DynamicResource SurfaceBrush}"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListViewItem">
                                                        <Border x:Name="Bd" 
                                                                Background="{TemplateBinding Background}" 
                                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Bd" Property="Background" Value="#2e1065"/>
                                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#8b5cf6"/>
                                                            </Trigger>
                                                            <MultiTrigger>
                                                                <MultiTrigger.Conditions>
                                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                                    <Condition Property="IsSelected" Value="False"/>
                                                                </MultiTrigger.Conditions>
                                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                                            </MultiTrigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>
                            </Border>

                            <!-- Pagination -->
                            <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="4" Padding="10,8" Margin="0,10,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <Button Content="⏮" Command="{Binding FirstPageCommand}" 
                                                Padding="8,4" Margin="0,0,4,0" FontSize="12"
                                                Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextSecondaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}" Cursor="Hand"/>
                                        <Button Content="◀" Command="{Binding PreviousPageCommand}" 
                                                Padding="10,4" FontSize="12"
                                                Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextSecondaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}" Cursor="Hand"/>
                                    </StackPanel>
                                    
                                    <TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                               FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}">
                                        <Run Text="Page "/>
                                        <Run Text="{Binding CurrentPage, Mode=OneWay}" FontWeight="SemiBold"/>
                                        <Run Text=" of "/>
                                        <Run Text="{Binding TotalPages, Mode=OneWay}" FontWeight="SemiBold"/>
                                        <Run Text=" ("/>
                                        <Run Text="{Binding TotalItemCount, Mode=OneWay}"/>
                                        <Run Text=" items)"/>
                                    </TextBlock>
                                    
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <Button Content="▶" Command="{Binding NextPageCommand}" 
                                                Padding="10,4" Margin="0,0,4,0" FontSize="12"
                                                Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextSecondaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}" Cursor="Hand"/>
                                        <Button Content="⏭" Command="{Binding LastPageCommand}" 
                                                Padding="8,4" FontSize="12"
                                                Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextSecondaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}" Cursor="Hand"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Stores Section -->
                <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="0">
                    <StackPanel>
                        <!-- Stores Header -->
                        <Border Background="#10b981" CornerRadius="8,8,0,0" Padding="15,12">
                            <TextBlock Text="Stores" FontSize="15" FontWeight="SemiBold" Foreground="White"/>
                        </Border>
                        
                        <StackPanel Margin="15">
                            <!-- Search -->
                            <TextBox Text="{Binding StoreSearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Padding="10,8" FontSize="13" Background="{DynamicResource SurfaceHoverBrush}" Foreground="{DynamicResource TextPrimaryBrush}" 
                                     BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="1" Margin="0,0,0,12"/>

                            <!-- Add/Edit Form -->
                            <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="Add/Edit Store" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                                    
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="70"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Store ID" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding NewStoreId, UpdateSourceTrigger=PropertyChanged}" 
                                                     Padding="8,6" FontSize="12" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Store Name" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding NewStoreName, UpdateSourceTrigger=PropertyChanged}" 
                                                     Padding="8,6" FontSize="12" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}"/>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="Rank" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                            <ComboBox ItemsSource="{Binding StoreRanks}" 
                                                      SelectedItem="{Binding NewStoreRank}" 
                                                      Padding="8,6" FontSize="12" Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource SurfaceActiveBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                        <Button Content="Add New" Command="{Binding AddStoreCommand}"
                                                Background="#10b981" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,0,8,0"/>
                                        <Button Content="Update Selected" Command="{Binding UpdateStoreCommand}"
                                                Background="#6366f1" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,0,8,0"/>
                                        <Button Content="Delete Selected" Command="{Binding DeleteStoreCommand}"
                                                Background="#ef4444" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,0,8,0"/>
                                        <Button Content="Clear Form" Command="{Binding ClearStoreFormCommand}"
                                                Background="{DynamicResource TextDisabledBrush}" Foreground="White" BorderThickness="0" 
                                                Padding="16,8" FontSize="12" Cursor="Hand"/>
                                    </StackPanel>
                                    <Button Content="📥 Import from Excel" Command="{Binding ImportStoresFromExcelCommand}"
                                            Background="#f59e0b" Foreground="White" BorderThickness="0" 
                                            Padding="16,8" FontSize="12" Cursor="Hand" Margin="0,10,0,0" HorizontalAlignment="Left"
                                            ToolTip="Import stores from Excel file (Code, Name, Rank columns)"/>
                                </StackPanel>
                            </Border>

                            <!-- Stores List Header -->
                            <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="4,4,0,0" Padding="8,6" BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="1,1,1,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="ID" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Grid.Column="1" Text="Name" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="10,0"/>
                                    <TextBlock Grid.Column="2" Text="Rank" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                </Grid>
                            </Border>
                            
                            <!-- Stores List -->
                            <Border BorderBrush="{DynamicResource SurfaceActiveBrush}" BorderThickness="1" CornerRadius="0,0,4,4">
                                <ListView ItemsSource="{Binding FilteredStores}"
                                          SelectedItem="{Binding SelectedStore}"
                                          ItemTemplate="{StaticResource StoreTemplate}"
                                          Height="250"
                                          BorderThickness="0"
                                          Background="{DynamicResource SurfaceBrush}"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListViewItem">
                                                        <Border x:Name="Bd" 
                                                                Background="{TemplateBinding Background}" 
                                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Bd" Property="Background" Value="#022c22"/>
                                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#10b981"/>
                                                            </Trigger>
                                                            <MultiTrigger>
                                                                <MultiTrigger.Conditions>
                                                                    <Condition Property="IsMouseOver" Value="True"/>
                                                                    <Condition Property="IsSelected" Value="False"/>
                                                                </MultiTrigger.Conditions>
                                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                                            </MultiTrigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>
                            </Border>

                            <!-- Store Count -->
                            <TextBlock Text="{Binding FilteredStores.Count, StringFormat='Total: {0} stores'}"
                                       FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,10,0,0" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
            </StackPanel>
        </ScrollViewer>
        <!-- Top shadow gradient -->
        <Border VerticalAlignment="Top" Height="16" IsHitTestVisible="False">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#12000000" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <!-- Bottom shadow gradient -->
        <Border VerticalAlignment="Bottom" Height="16" IsHitTestVisible="False">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="Transparent" Offset="0"/>
                    <GradientStop Color="#12000000" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
    </Grid>
</UserControl>





