<Window x:Class="SAP.Windows.NotesWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:SAP.Features.NotesTracker.Views"
        Title="Notes Widget" 
        Height="800" 
        Width="350"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="20" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="{DynamicResource SurfaceBrush}"
                    CornerRadius="12,12,0,0"
                    Padding="12,8"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="📝 Notes"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Button Content="+"
                                Click="AddNote_Click"
                                Width="28"
                                Height="28"
                                FontSize="16"
                                FontWeight="Bold"
                                Margin="0,0,8,0"
                                Style="{DynamicResource GhostButtonStyle}"
                                ToolTip="Add Note"/>
                        <Button Content="×"
                                Click="Close_Click"
                                Width="28"
                                Height="28"
                                FontSize="18"
                                FontWeight="Bold"
                                Style="{DynamicResource GhostButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content -->
            <views:NotesWidgetView x:Name="NotesView" Grid.Row="1"/>
        </Grid>
    </Border>
</Window>
