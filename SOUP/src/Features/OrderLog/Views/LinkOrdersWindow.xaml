<Window x:Class="SOUP.Features.OrderLog.Views.LinkOrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:SOUP.Features.OrderLog.Models"
        Title="Link Orders" Height="600" Width="720" WindowStartupLocation="CenterOwner" ResizeMode="CanResize" MinHeight="400" MinWidth="600">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SOUP;component/Themes/ModernStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Status to Color Converter -->
            <Style x:Key="StatusIndicatorStyle" TargetType="Border">
            <Setter Property="Width" Value="4"/>
            <Setter Property="CornerRadius" Value="2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:OrderItem+OrderStatus.NotReady}">
                    <Setter Property="Background" Value="#F44336"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:OrderItem+OrderStatus.OnDeck}">
                    <Setter Property="Background" Value="#FF9800"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:OrderItem+OrderStatus.InProgress}">
                    <Setter Property="Background" Value="#2196F3"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:OrderItem+OrderStatus.Done}">
                    <Setter Property="Background" Value="#4CAF50"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Order Card Template -->
        <DataTemplate x:Key="OrderCardTemplate">
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Margin="4"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Status Color Indicator -->
                    <Border Grid.Column="0" Style="{StaticResource StatusIndicatorStyle}" Margin="0,0,10,0"/>

                    <!-- Order Info -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding DisplayTitle}" FontWeight="SemiBold" FontSize="{DynamicResource CardFontSize}"/>
                        <TextBlock Margin="0,2,0,0" FontSize="11" Opacity="0.7">
                            <Run Text="Vendor:"/>
                            <Run Text="{Binding VendorName, TargetNullValue='(none)', Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock FontSize="11" Opacity="0.7">
                            <Run Text="Transfer:"/>
                            <Run Text="{Binding TransferNumbers, TargetNullValue='(none)', Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock FontSize="11" Opacity="0.7">
                            <Run Text="Status:"/>
                            <Run Text="{Binding Status, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Linked Group Badge -->
                    <Border Grid.Column="2"
                            Background="{DynamicResource AccentBrush}"
                            CornerRadius="4"
                            Padding="6,3"
                            Margin="8,0,0,0"
                            Visibility="{Binding LinkedGroupId, Converter={StaticResource NullToVisibilityConverter}}">
                        <TextBlock Text="🔗 Linked" FontSize="10" Foreground="White"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,12">
            <TextBlock Text="Link Orders" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Text="Select one or more orders to link together. Linked orders will be displayed as a merged card with unified status."
                       FontSize="11" Opacity="0.7" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Search Box -->
        <TextBox x:Name="SearchBox"
                 Grid.Row="1"
                 Grid.Column="0"
                 Margin="0,0,0,8"
                 Padding="8,6"
                 TextChanged="SearchBox_TextChanged">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                        <VisualBrush.Visual>
                                            <TextBlock Text="🔍 Search by vendor, transfer #, or shipment #..."
                                                       Opacity="0.5"
                                                       Margin="8,0,0,0"/>
                                        </VisualBrush.Visual>
                                    </VisualBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Filter Buttons -->
        <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,8">
            <CheckBox x:Name="ShowLinkedCheckBox"
                      Content="Show linked"
                      IsChecked="True"
                      Checked="FilterChanged"
                      Unchecked="FilterChanged"
                      Margin="0,0,12,0"/>
            <TextBlock Text="{Binding ElementName=OrdersList, Path=Items.Count, StringFormat='{}{0} orders'}"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Opacity="0.7"/>
        </StackPanel>

        <!-- Orders List -->
        <Border Grid.Row="2"
                Grid.Column="0"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Margin="0,0,8,0">
            <ListBox x:Name="OrdersList"
                     SelectionMode="Extended"
                     SelectionChanged="OrdersList_SelectionChanged"
                     ItemTemplate="{StaticResource OrderCardTemplate}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="4"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <!-- Preview Panel -->
        <Border Grid.Row="2"
                Grid.Column="2"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="8,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Preview Header -->
                <TextBlock Grid.Row="0"
                           Text="Preview"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>

                <!-- Preview Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PreviewPanel">
                        <TextBlock Text="Select orders to preview the linked group"
                                   Opacity="0.5"
                                   FontSize="11"
                                   TextWrapping="Wrap"
                                   x:Name="PreviewPlaceholder"/>

                        <ItemsControl x:Name="PreviewList" Visibility="Collapsed">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceHoverBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,0,0,1"
                                            Padding="8,6"
                                            Margin="0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Style="{StaticResource StatusIndicatorStyle}" Margin="0,0,8,0"/>

                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayTitle}" FontWeight="SemiBold" FontSize="11"/>
                                                <TextBlock Text="{Binding VendorName, TargetNullValue='(no vendor)', Mode=OneWay}"
                                                           FontSize="10"
                                                           Opacity="0.7"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Selection Info -->
        <TextBlock Grid.Row="3"
                   Grid.Column="0"
                   Grid.ColumnSpan="3"
                   Margin="0,12,0,8"
                   FontSize="11"
                   Opacity="0.7">
            <Run Text="💡 Tip:"/>
            <Run Text="Hold Ctrl to select multiple orders. Selected orders will be merged into a single card with unified status."/>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CancelBtn"
                    Content="Cancel"
                    Width="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="CancelBtn_Click"/>
            <Button x:Name="LinkBtn"
                    Content="Link Orders"
                    Width="120"
                    Height="32"
                    Click="LinkBtn_Click"
                    IsEnabled="False">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
