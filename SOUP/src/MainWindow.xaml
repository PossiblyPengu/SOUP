<Window x:Class="SOUP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:vm="clr-namespace:SOUP.ViewModels"
        xmlns:models="clr-namespace:SOUP.Models"
        xmlns:converters="clr-namespace:SOUP.Converters"
        xmlns:behaviors="clr-namespace:SOUP.Behaviors"
        xmlns:core="clr-namespace:SOUP.Core"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        Title="{Binding Title}"
        Width="1200" Height="800"
        MinWidth="1000" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent">

    <Window.InputBindings>
        <!-- Module Navigation Shortcuts -->
        <KeyBinding Key="D1" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchExpireWiseCommand}"/>
        <KeyBinding Key="D2" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchAllocationBuddyCommand}"/>
        <KeyBinding Key="D3" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchEssentialsBuddyCommand}"/>
        <KeyBinding Key="D4" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchSwiftLabelCommand}"/>
        <KeyBinding Key="D5" Modifiers="Alt" Command="{Binding LauncherViewModel.LaunchOrderLogCommand}"/>
        <KeyBinding Key="W" Modifiers="Ctrl+Alt" Command="{Binding LauncherViewModel.OpenOrderLogWidgetCommand}"/>
        
        <!-- Global Shortcuts -->
        <KeyBinding Key="T" Modifiers="Ctrl" Command="{Binding ToggleThemeCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding NavigationService.NavigateBackCommand}"/>
        <KeyBinding Key="H" Modifiers="Alt" Command="{Binding NavigationService.NavigateBackCommand}"/>
        <KeyBinding Key="F1" Command="{Binding ShowAboutCommand}"/>
    </Window.InputBindings>

    <Window.Resources>
        <converters:ViewModelToViewConverter x:Key="ViewModelToView"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <!-- Custom Window Chrome -->
        <Border Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="{DynamicResource WindowCornerRadius}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3" Color="#000000"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource TitleBarBackgroundBrush}"
                    CornerRadius="{DynamicResource TitleBarCornerRadius}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="⚙" FontSize="16" Foreground="{DynamicResource TitleBarTextBrush}" Margin="0,0,8,0" FontFamily="Segoe UI Emoji"/>
                        <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TitleBarTextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Update Available Banner -->
                    <Border Grid.Column="1" Background="#2D7D46" CornerRadius="4" Padding="10,4" Margin="8,0"
                            VerticalAlignment="Center" Cursor="Hand"
                            Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BoolToVisibility}}">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding ShowUpdateCommand}"/>
                        </Border.InputBindings>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center" FontFamily="Segoe UI Emoji"/>
                            <TextBlock Text="Update available: v" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding UpdateVersion}" Foreground="White" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Content="✕" FontSize="10" Margin="10,0,0,0" Padding="4,2" VerticalAlignment="Center"
                                    Background="Transparent" Foreground="White" BorderThickness="0" Cursor="Hand"
                                    Command="{Binding DismissUpdateCommand}" ToolTip="Dismiss">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Opacity" Value="0.7"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>
                    
                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,8,0">
                        <!-- Minimize -->
                        <Button Click="MinimizeButton_Click" ToolTip="Minimize"
                                Width="36" Height="28" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource XSmallCornerRadius}">
                                        <TextBlock Text="─" FontSize="12" Foreground="{DynamicResource TitleBarTextBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        
                        <!-- Maximize/Restore -->
                        <Button Click="MaximizeButton_Click" ToolTip="Maximize"
                                Width="36" Height="28" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource XSmallCornerRadius}">
                                        <TextBlock x:Name="MaxIcon" Text="□" FontSize="12" Foreground="{DynamicResource TitleBarTextBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        
                        <!-- Close -->
                        <Button Click="CloseButton_Click" ToolTip="Close"
                                Width="36" Height="28" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource XSmallCornerRadius}">
                                        <TextBlock Text="✕" FontSize="11" Foreground="{DynamicResource TitleBarTextBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                                            <Setter TargetName="Bd" Property="TextBlock.Foreground" Value="{DynamicResource CloseButtonHoverTextBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Main Content Area -->
            <Grid Grid.Row="1">
        <!-- Launcher View -->
        <Grid x:Name="LauncherPanel"
              Visibility="{Binding NavigationService.CurrentView, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar - App Selection -->
            <Border Grid.Column="0" Background="{DynamicResource SidebarBackgroundBrush}" CornerRadius="{DynamicResource SidebarCornerRadius}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Logo & Branding -->
                    <StackPanel Grid.Row="0" Margin="24,32,24,24">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Width="56" Height="56" CornerRadius="{DynamicResource AppLogoCornerRadius}"
                                Background="{DynamicResource AppLogoGradient}"
                                Style="{DynamicResource LogoBounceStyle}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.4" Color="{DynamicResource AppLogoShadowColor}"/>
                                </Border.Effect>
                                <Image Source="pack://application:,,,/Assets/soup-logo.png" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center" Margin="14,0,0,0">
                                <TextBlock Text="S.O.U.P" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="S.A.M. Operations Utilities Pack" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <!-- Version Badge -->
                        <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="{DynamicResource SmallCornerRadius}" Padding="12,8" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{DynamicResource StatusOnlineBrush}"/>
                                <TextBlock Text="{x:Static core:AppVersion.DisplayVersion}" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                                <TextBlock Text="  •  " Foreground="{DynamicResource TextDisabledBrush}" FontSize="12"/>
                                <TextBlock Text="{x:Static core:AppVersion.Channel}" Foreground="{DynamicResource StatusOnlineBrush}" FontSize="12" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- App Selection List -->
                    <StackPanel Grid.Row="1" Margin="0,8,0,0">
                        <TextBlock Text="APPLICATIONS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextDisabledBrush}" 
                                   Margin="24,0,0,12"/>

                        <ListBox x:Name="NavListBox"
                                 ItemsSource="{Binding NavItems}"
                                 SelectedItem="{Binding SelectedNavItem, Mode=TwoWay}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <i:Interaction.Behaviors>
                                <behaviors:ListBoxDragDropBehavior OnReorder="{Binding SaveNavOrderAction}"/>
                            </i:Interaction.Behaviors>
                        
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVisibility}}"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:NavItem}">
                                    <Grid>
                                        <RadioButton GroupName="AppNav" 
                                                     Style="{DynamicResource AppNavButtonStyle}"
                                                     IsChecked="{Binding IsSelected, Mode=TwoWay}">
                                            <RadioButton.Tag>
                                                <TextBlock Text="{Binding IconKey, Converter={StaticResource ResourceKeyConverter}}" FontSize="20" FontFamily="Segoe UI Emoji"/>
                                            </RadioButton.Tag>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}"/>
                                            </StackPanel>
                                        </RadioButton>
                                        <!-- Drag Handle Indicator -->
                                        <StackPanel Orientation="Vertical" 
                                                    HorizontalAlignment="Right" 
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,16,0"
                                                    Opacity="0.35"
                                                    Tag="DragHandle"
                                                    Cursor="SizeAll"
                                                    Background="Transparent">
                                            <Border Width="4" Height="4" CornerRadius="2" Background="{DynamicResource TextTertiaryBrush}" Margin="0,1"/>
                                            <Border Width="4" Height="4" CornerRadius="2" Background="{DynamicResource TextTertiaryBrush}" Margin="0,1"/>
                                            <Border Width="4" Height="4" CornerRadius="2" Background="{DynamicResource TextTertiaryBrush}" Margin="0,1"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>

                    <!-- Bottom Actions -->
                    <StackPanel Grid.Row="2" Margin="12,0,12,20">
                        <Separator Background="{DynamicResource SurfaceHoverBrush}" Margin="12,0,12,16"/>
                        
                        <!-- Theme Toggle -->
                        <Button Command="{Binding ToggleThemeCommand}" Style="{DynamicResource QuickActionStyle}" Margin="0,0,0,8"
                                ToolTip="Toggle Light/Dark Mode">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="16" Margin="0,0,12,0" FontFamily="Segoe UI Emoji">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🌙"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkMode}" Value="False">
                                                    <Setter Property="Text" Value="☀️"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock FontSize="14" FontWeight="Medium" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Dark Mode"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkMode}" Value="False">
                                                    <Setter Property="Text" Value="Light Mode"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Button>
                        
                        <Button Click="SettingsButton_Click" Style="{DynamicResource QuickActionStyle}" Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" FontSize="16" Margin="0,0,12,0" FontFamily="Segoe UI Emoji"/>
                                <TextBlock Text="Settings" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Keyboard Shortcuts Help -->
                        <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="{DynamicResource BadgeCornerRadius}" Padding="12,10" Margin="0,12,0,0">
                            <StackPanel>
                                <TextBlock Text="SHORTCUTS" FontSize="9" FontWeight="Bold" 
                                           Foreground="{DynamicResource TextDisabledBrush}" Margin="0,0,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Alt+1-4" FontSize="10" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,4"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Launch module" FontSize="10" 
                                               Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                    
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl+T" FontSize="10" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,4"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="Toggle theme" FontSize="10" 
                                               Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                    
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Esc" FontSize="10" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,12,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="Return to home" FontSize="10" 
                                               Foreground="{DynamicResource TextTertiaryBrush}"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right Side - Splash Screen -->
            <Grid Grid.Column="1">
                <!-- ExpireWise Splash -->
                <!-- ExpireWise Splash -->
                <Grid Visibility="{Binding SelectedNavItem, Converter={StaticResource NavItemIdToVisibility}, ConverterParameter=ExpireWise}"
                      Background="{DynamicResource ExpireWiseSplashGradient}">
                    
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500">
                        <Border Width="120" Height="120" CornerRadius="{DynamicResource SplashIconCornerRadius}" HorizontalAlignment="Center" Margin="0,0,0,32"
                                Background="{DynamicResource ExpireWiseIconGradient}"
                                Style="{DynamicResource FloatingSplashIconStyle}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.6" Color="{DynamicResource ExpireWiseIconShadowColor}"/>
                            </Border.Effect>
                            <TextBlock Text="{DynamicResource ExpireWiseSplashIcon}" FontSize="56" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="ExpireWise" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource SplashTitleBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Track product expiration dates and manage inventory lifecycle" 
                                   FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" Margin="0,0,0,24"/>
                        
                        <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="📅 Expiry Tracking" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="📊 Analytics" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="🔔 Smart Alerts" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                        </WrapPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Command="{Binding LauncherViewModel.LaunchExpireWiseCommand}" 
                                    Style="{DynamicResource LaunchButtonStyle}"/>
                            <Button Command="{Binding LauncherViewModel.PopOutExpireWiseCommand}"
                                    Style="{DynamicResource PopOutButtonStyle}" Margin="12,0,0,0"
                                    ToolTip="Open in new window"/>
                        </StackPanel>
                        
                        <TextBlock Text="Press Alt+1 for quick launch" FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" 
                                   HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Allocation Buddy Splash -->
                <Grid Visibility="{Binding SelectedNavItem, Converter={StaticResource NavItemIdToVisibility}, ConverterParameter=AllocationBuddy}"
                      Background="{DynamicResource AllocationBuddySplashGradient}">
                    
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500">
                        <Border Width="120" Height="120" CornerRadius="{DynamicResource SplashIconCornerRadius}" HorizontalAlignment="Center" Margin="0,0,0,32"
                                Background="{DynamicResource AllocationBuddyIconGradient}"
                                Style="{DynamicResource FloatingSplashIconStyle}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.6" Color="{DynamicResource AllocationBuddyIconShadowColor}"/>
                            </Border.Effect>
                            <TextBlock Text="{DynamicResource AllocationBuddySplashIcon}" FontSize="56" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="Allocation Buddy" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource SplashTitleBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Smart inventory allocation and store distribution management" 
                                   FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" Margin="0,0,0,24"/>
                        
                        <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="🏪 Multi-Store" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="📋 BC Ready" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="📥 Excel Import" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                        </WrapPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Command="{Binding LauncherViewModel.LaunchAllocationBuddyCommand}" 
                                    Style="{DynamicResource LaunchButtonStyle}"/>
                            <Button Command="{Binding LauncherViewModel.PopOutAllocationBuddyCommand}"
                                    Style="{DynamicResource PopOutButtonStyle}" Margin="12,0,0,0"
                                    ToolTip="Open in new window"/>
                        </StackPanel>
                        
                        <TextBlock Text="Press Alt+2 for quick launch" FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" 
                                   HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Essentials Buddy Splash -->
                <Grid Visibility="{Binding SelectedNavItem, Converter={StaticResource NavItemIdToVisibility}, ConverterParameter=EssentialsBuddy}"
                      Background="{DynamicResource EssentialsBuddySplashGradient}">
                    
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500">
                        <Border Width="120" Height="120" CornerRadius="{DynamicResource SplashIconCornerRadius}" HorizontalAlignment="Center" Margin="0,0,0,32"
                                Background="{DynamicResource EssentialsBuddyIconGradient}"
                                Style="{DynamicResource FloatingSplashIconStyle}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.6" Color="{DynamicResource EssentialsBuddyIconShadowColor}"/>
                            </Border.Effect>
                            <TextBlock Text="{DynamicResource EssentialsBuddySplashIcon}" FontSize="56" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="Essentials Buddy" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource SplashTitleBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Monitor essential inventory levels and track stock status" 
                                   FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" Margin="0,0,0,24"/>
                        
                        <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="⭐ Essential Tracking" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="🎨 Status Colors" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="⚠️ Misplaced Alerts" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                        </WrapPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Command="{Binding LauncherViewModel.LaunchEssentialsBuddyCommand}" 
                                    Style="{DynamicResource LaunchButtonStyle}"/>
                            <Button Command="{Binding LauncherViewModel.PopOutEssentialsBuddyCommand}"
                                    Style="{DynamicResource PopOutButtonStyle}" Margin="12,0,0,0"
                                    ToolTip="Open in new window"/>
                        </StackPanel>
                        
                        <TextBlock Text="Press Alt+3 for quick launch" FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" 
                                   HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Swift Label Splash -->
                <Grid Visibility="{Binding SelectedNavItem, Converter={StaticResource NavItemIdToVisibility}, ConverterParameter=SwiftLabel}"
                      Background="{DynamicResource SwiftLabelSplashGradient}">
                    
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500">
                        <Border Width="120" Height="120" CornerRadius="{DynamicResource SplashIconCornerRadius}" HorizontalAlignment="Center" Margin="0,0,0,32"
                                Background="{DynamicResource SwiftLabelIconGradient}"
                                Style="{DynamicResource FloatingSplashIconStyle}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.6" Color="{DynamicResource SwiftLabelIconShadowColor}"/>
                            </Border.Effect>
                            <TextBlock Text="{DynamicResource SwiftLabelSplashIcon}" FontSize="56" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <TextBlock Text="Swift Label" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource SplashTitleBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Generate transfer order box labels quickly and easily" 
                                   FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" Margin="0,0,0,24"/>
                        
                        <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="🖨️ Quick Print" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="📦 Box Labels" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="🏪 Store Info" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                        </WrapPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Command="{Binding LauncherViewModel.LaunchSwiftLabelCommand}" 
                                    Style="{DynamicResource LaunchButtonStyle}"/>
                            <Button Command="{Binding LauncherViewModel.PopOutSwiftLabelCommand}"
                                    Style="{DynamicResource PopOutButtonStyle}" Margin="12,0,0,0"
                                    ToolTip="Open in new window"/>
                        </StackPanel>
                        
                        <TextBlock Text="Press Alt+4 for quick launch" FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" 
                                   HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Order Log Splash -->
                <Grid Visibility="{Binding SelectedNavItem, Converter={StaticResource NavItemIdToVisibility}, ConverterParameter=OrderLog}"
                      Background="{DynamicResource OrderLogSplashGradient}">
                    
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500">
                        <!-- Large Icon -->
                        <Border Width="120" Height="120" CornerRadius="{DynamicResource SplashIconCornerRadius}" HorizontalAlignment="Center" Margin="0,0,0,32"
                                Background="{DynamicResource OrderLogIconGradient}"
                                Style="{DynamicResource FloatingSplashIconStyle}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="50" ShadowDepth="0" Opacity="0.6" Color="{DynamicResource OrderLogIconShadowColor}"/>
                            </Border.Effect>
                            <TextBlock Text="{DynamicResource OrderLogSplashIcon}" FontSize="56" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <!-- Title -->
                        <TextBlock Text="Order Log" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource SplashTitleBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        
                        <!-- Subtitle -->
                        <TextBlock Text="Track transfer groups, mark orders complete, and colour-code entries" 
                                   FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" TextAlignment="Center" TextWrapping="Wrap"
                                   HorizontalAlignment="Center" Margin="0,0,0,24"/>
                        
                        <!-- Features -->
                        <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,40">
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="📋 Transfer Groups" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="🎨 Colour Coding" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                            <Border Style="{DynamicResource AnimatedFeatureBadgeStyle}">
                                <TextBlock Text="✅ Status Tracking" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium"/>
                            </Border>
                        </WrapPanel>
                        
                        <!-- Launch Buttons (Launch opens widget; small button opens settings) -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Command="{Binding LauncherViewModel.OpenOrderLogWidgetCommand}" 
                                    Style="{DynamicResource LaunchButtonStyle}"/>
                            <Button Command="{Binding LauncherViewModel.LaunchOrderLogCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}" Margin="12,0,0,0"
                                    Padding="12,8"
                                    ToolTip="Open settings for Order Log">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚙" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="Settings" FontSize="13"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        
                        <!-- Keyboard shortcut hint -->
                        <TextBlock Text="Press Alt+5 for quick launch" FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" 
                                   HorizontalAlignment="Center" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>

        <!-- Module View (shown when a module is active) -->
        <Grid Visibility="{Binding NavigationService.CurrentView, Converter={StaticResource NullToVisibility}}"
              Background="{DynamicResource BackgroundBrush}">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Navigation Bar -->
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Back Button -->
                    <Button Grid.Column="0" HorizontalAlignment="Left"
                            Command="{Binding NavigationService.NavigateBackCommand}"
                            Cursor="Hand" Background="Transparent" BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Border" Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="{DynamicResource BadgeCornerRadius}" Padding="14,10">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="←" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        <TextBlock Text="Back to Launcher" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource SurfaceActiveBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource BorderDarkBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <!-- Widget (OrderLog) + Theme Toggle -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Command="{Binding LauncherViewModel.OpenOrderLogWidgetCommand}"
                                Visibility="{Binding SelectedNavItem, Converter={StaticResource NavItemIdToVisibility}, ConverterParameter=OrderLog}"
                                Style="{DynamicResource SecondaryButtonStyle}"
                                ToolTip="Open docked widget (Ctrl+Alt+W)"
                                Margin="0,0,8,0" Padding="10,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="▫" FontSize="14" Margin="0,0,6,0"/>
                                <TextBlock Text="Widget" FontSize="13"/>
                            </StackPanel>
                        </Button>

                        <!-- Theme Toggle Button -->
                        <Button Command="{Binding ToggleThemeCommand}"
                                Cursor="Hand" Background="Transparent" BorderThickness="0"
                                ToolTip="Toggle Light/Dark Mode">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border" Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="{DynamicResource BadgeCornerRadius}" Padding="12,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="16" VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="🌙"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDarkMode}" Value="False">
                                                                <Setter Property="Text" Value="☀️"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBlock FontSize="13" FontWeight="Medium"
                                                       VerticalAlignment="Center" Margin="8,0,0,0"
                                                       Foreground="{DynamicResource TextSecondaryBrush}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Dark"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDarkMode}" Value="False">
                                                                <Setter Property="Text" Value="Light"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SurfaceActiveBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDarkMode}" Value="False">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="☀️" FontSize="16"/>
                                                        <TextBlock Text="Light" FontSize="13" FontWeight="Medium" Margin="8,0,0,0"/>
                                                    </StackPanel>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Module Content -->
            <ContentControl Grid.Row="1"
                            Content="{Binding NavigationService.CurrentView, Converter={StaticResource ViewModelToView}}"
                            behaviors:ContentTransitionBehavior.EnableTransition="True"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"/>
        </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>

