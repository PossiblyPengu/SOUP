<UserControl x:Class="SOUP.Views.EssentialsBuddy.EssentialsBuddyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SOUP.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=vm:EssentialsBuddyViewModel}"
             Focusable="True">

    <!-- Keyboard Shortcuts -->
    <UserControl.InputBindings>
        <KeyBinding Key="I" Modifiers="Ctrl" Command="{Binding ImportCommand}"/>
        <KeyBinding Key="E" Modifiers="Ctrl" Command="{Binding ExportCommand}"/>
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding FocusSearchCommand}"/>
        <KeyBinding Key="R" Modifiers="Ctrl" Command="{Binding RefreshCommand}"/>
    </UserControl.InputBindings>

    <UserControl.Resources>
        <!-- Emerald green accent for EssentialsBuddy -->
        <Color x:Key="EssentialsAccentColor">#10b981</Color>
        <Color x:Key="EssentialsAccentLightColor">#34d399</Color>
        <SolidColorBrush x:Key="EssentialsAccentBrush" Color="{StaticResource EssentialsAccentColor}"/>
        <SolidColorBrush x:Key="EssentialsAccentLightBrush" Color="{StaticResource EssentialsAccentLightColor}"/>
        
        <!-- Stock level colors now come from theme (DynamicResource) -->
        
        <!-- Sidebar Navigation Style -->
        <Style x:Key="SidebarNavItem" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SidebarActiveBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource EssentialsAccentLightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Sidebar Action Button Style (for non-toggle actions) -->
        <Style x:Key="SidebarActionItem" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SidebarActiveBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid Background="{DynamicResource BackgroundBrush}">
        <!-- Full Screen Welcome Screen (shown when no data) -->
        <Grid Visibility="{Binding HasNoData, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="{DynamicResource WelcomeScreenGradientBrush}">
            
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                            MaxWidth="600" Margin="24" MinHeight="400">
                    <!-- Icon -->
                    <Border Width="100" Height="100" CornerRadius="28" 
                            HorizontalAlignment="Center" Margin="0,0,0,24">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#10b981" Offset="0"/>
                                <GradientStop Color="#34d399" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.6" Color="#10b981"/>
                        </Border.Effect>
                        <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    
                    <!-- Title -->
                    <TextBlock Text="Essentials Buddy" FontSize="36" FontWeight="Bold"
                               Foreground="{DynamicResource SplashTitleBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"
                               TextWrapping="Wrap" TextAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    
                    <!-- Subtitle -->
                    <TextBlock Text="Track and manage your essential inventory items" FontSize="16"
                               Foreground="{DynamicResource TextTertiaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,32"
                               TextWrapping="Wrap" TextAlignment="Center"/>
                    
                    <!-- Import Button -->
                    <Button Command="{Binding ImportFromExcelCommand}" 
                            HorizontalAlignment="Center" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Padding" Value="32,14"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Bg" CornerRadius="14" Padding="{TemplateBinding Padding}">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#10b981" Offset="0"/>
                                                        <GradientStop Color="#34d399" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.4" Color="#10b981"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                <GradientStop Color="#34d399" Offset="0"/>
                                                                <GradientStop Color="#6ee7b7" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📥" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Import Inventory File" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    
                    <!-- Features -->
                    <WrapPanel HorizontalAlignment="Center" Margin="0,32,0,0">
                        <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="10" Padding="14,10" Margin="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⭐" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Essential Tracking" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="Medium" FontSize="13"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="10" Padding="14,10" Margin="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎨" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Color-Coded Status" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="Medium" FontSize="13"/>
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="10" Padding="14,10" Margin="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚠️" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Misplaced Alerts" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="Medium" FontSize="13"/>
                            </StackPanel>
                        </Border>
                    </WrapPanel>
                    
                    <!-- Info -->
                    <TextBlock Text="Import Excel (.xlsx) files • Only 9-90* bin items will be shown" 
                               FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" HorizontalAlignment="Center" Margin="0,20,0,0"
                               TextWrapping="Wrap" TextAlignment="Center"/>
                    <TextBlock Text="All 208 essential items will be included automatically" 
                               FontSize="12" Foreground="{DynamicResource TextDisabledBrush}" HorizontalAlignment="Center" Margin="0,6,0,0"
                               TextWrapping="Wrap" TextAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Main Content with Sidebar (shown when has data) -->
        <Grid Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource SurfaceHoverBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="16,20,16,16">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Border Width="36" Height="36" CornerRadius="10" Margin="0,0,12,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#10b981" Offset="0"/>
                                        <GradientStop Color="#34d399" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock Text="📋" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Essentials" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Buddy" FontSize="12" Foreground="{DynamicResource TextTertiaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- Stats Summary -->
                    <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" CornerRadius="12" Margin="12,0,12,12" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="{Binding FilteredItems.Count}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource SuccessLightBrush}"/>
                                <TextBlock Text="Total Items" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="{Binding EssentialItemsCount, FallbackValue=0}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource WarningLightBrush}"/>
                                <TextBlock Text="Essential" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="0" Grid.Row="1">
                                <TextBlock Text="{Binding OutOfStockCount, FallbackValue=0}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}"/>
                                <TextBlock Text="Out of Stock" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Grid.Row="1">
                                <TextBlock Text="{Binding LowStockCount, FallbackValue=0}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource WarningBrush}"/>
                                <TextBlock Text="Low Stock" FontSize="10" Foreground="{DynamicResource TextTertiaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Navigation -->
                    <StackPanel Grid.Row="2" Margin="0,8,0,0">
                        <TextBlock Text="NAVIGATION" FontSize="10" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextDisabledBrush}" Margin="20,0,0,8"/>
                        
                        <RadioButton x:Name="NavData" Style="{StaticResource SidebarNavItem}" 
                                     IsChecked="True" GroupName="EssentialsNav">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Data View" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                        
                        <Separator Background="{DynamicResource SurfaceHoverBrush}" Margin="16,12"/>
                        
                        <TextBlock Text="ACTIONS" FontSize="10" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextDisabledBrush}" Margin="20,0,0,8"/>
                        
                        <Button Style="{StaticResource SidebarActionItem}"
                                Command="{Binding ImportFromExcelCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📥" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Import Data" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource SidebarActionItem}"
                                Command="{Binding ExportToExcelCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📤" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Export Excel" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource SidebarActionItem}"
                                Command="{Binding ExportToCsvCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📄" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Export CSV" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource SidebarActionItem}"
                                Command="{Binding LoadItemsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔄" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Refresh Data" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Bottom Actions -->
                    <StackPanel Grid.Row="3" Margin="8,0,8,12">
                        <Separator Background="{DynamicResource SurfaceHoverBrush}" Margin="8,0,8,12"/>
                        
                        <Button Style="{StaticResource SidebarActionItem}"
                                Command="{Binding OpenSettingsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Settings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Style="{StaticResource SidebarActionItem}"
                                Command="{Binding ClearDataCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🗑️" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="Clear Data" VerticalAlignment="Center" Foreground="{DynamicResource DangerBrush}"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Column="1" Background="{DynamicResource BackgroundBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with Search -->
                <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="20,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Search Box -->
                        <Border Grid.Column="0" Background="{DynamicResource SurfaceHoverBrush}" CornerRadius="10" Padding="12,0" MaxWidth="500" HorizontalAlignment="Left">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🔍" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox x:Name="SearchBox" Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}"
                                         VerticalAlignment="Center" Padding="0,8" FontSize="14"
                                         CaretBrush="{DynamicResource TextPrimaryBrush}">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                                <VisualBrush.Visual>
                                                                    <TextBlock Text="Search items..." Foreground="{DynamicResource TextTertiaryBrush}"/>
                                                                </VisualBrush.Visual>
                                                            </VisualBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                            </Grid>
                        </Border>

                        <!-- Status Filter -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0">
                            <TextBlock Text="Status:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox SelectedValuePath="Content" SelectedValue="{Binding StatusFilter}"
                                      Style="{StaticResource ModernComboBox}" Width="140">
                                <ComboBoxItem>All</ComboBoxItem>
                                <ComboBoxItem>No Stock</ComboBoxItem>
                                <ComboBoxItem>Low Stock</ComboBoxItem>
                                <ComboBoxItem>Sufficient</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>

                        <!-- Essentials Toggle -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <CheckBox Content="Essentials Only" 
                                      IsChecked="{Binding EssentialsOnly}"
                                      Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"
                                      VerticalAlignment="Center"/>
                            
                            <!-- Include PL checkbox removed per UX request -->
                            
                            <Border Background="{DynamicResource SuccessBrush}" CornerRadius="10" Padding="12,6" Margin="16,0,0,0">
                                <TextBlock Text="{Binding FilteredItems.Count, StringFormat='{}{0} items'}"
                                           Foreground="{DynamicResource TextOnPrimaryBrush}" FontWeight="SemiBold" FontSize="12"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Quick Actions Bar -->
                <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" Padding="16,8" BorderBrush="{DynamicResource SurfaceHoverBrush}" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding EditItemCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✏️" FontSize="12" Margin="0,0,6,0"/>
                                <TextBlock Text="Edit" FontSize="12"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding DeleteItemCommand}" Style="{StaticResource DangerButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🗑️" FontSize="12" Margin="0,0,6,0"/>
                                <TextBlock Text="Delete" FontSize="12"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding SaveSnapshotCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💾" FontSize="12" Margin="0,0,6,0"/>
                                <TextBlock Text="Save Snapshot" FontSize="12"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding CompareWithPreviousRunCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" FontSize="12" Margin="0,0,6,0"/>
                                <TextBlock Text="Compare Last Run" FontSize="12"/>
                            </StackPanel>
                        </Button>

                        <!-- Legend -->
                        <StackPanel Orientation="Horizontal" Margin="32,0,0,0">
                            <TextBlock Text="Legend:" Foreground="{DynamicResource TextTertiaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            
                            <Border Background="{DynamicResource OutOfStockBrush}" CornerRadius="4" Padding="8,3" Margin="0,0,6,0">
                                <TextBlock Text="No Stock" Foreground="{DynamicResource OutOfStockTextBrush}" FontSize="10"/>
                            </Border>
                            
                            <Border Background="{DynamicResource LowStockBrush}" CornerRadius="4" Padding="8,3" Margin="0,0,6,0">
                                <TextBlock Text="Low Stock" Foreground="{DynamicResource LowStockTextBrush}" FontSize="10"/>
                            </Border>
                            
                            <Border Background="{DynamicResource SufficientBrush}" CornerRadius="4" Padding="8,3" Margin="0,0,6,0">
                                <TextBlock Text="Sufficient" Foreground="{DynamicResource SufficientTextBrush}" FontSize="10"/>
                            </Border>
                            
                            <Border Background="{DynamicResource DangerBrush}" CornerRadius="4" Padding="8,3" Margin="0,0,6,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚠" FontSize="12" Foreground="{DynamicResource TextOnPrimaryBrush}" Margin="0,0,6,0"/>
                                    <TextBlock Text="Non-Essential in 9-90" Foreground="{DynamicResource TextOnPrimaryBrush}" FontSize="10" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Data Grid -->
                <DataGrid x:Name="ItemsGrid" Grid.Row="2" ItemsSource="{Binding FilteredItems}" SelectedItem="{Binding SelectedItem}"
                          Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource EssentialsDataGridColumnHeader}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          CanUserReorderColumns="True" CanUserResizeColumns="True"
                          CanUserSortColumns="True" Margin="16"
                          ColumnWidth="SizeToCells"
                          HorizontalScrollBarVisibility="Auto">
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditItemCommand}"/>
                    </DataGrid.InputBindings>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource ModernDataGridRow}">
                            <Style.Triggers>
                                <!-- Out of Stock (red) -->
                                <DataTrigger Binding="{Binding Status}" Value="OutOfStock">
                                    <Setter Property="Background" Value="{DynamicResource OutOfStockBrush}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource OutOfStockAccentBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource OutOfStockTextBrush}"/>
                                </DataTrigger>
                                <!-- Low Stock (orange) -->
                                <DataTrigger Binding="{Binding Status}" Value="Low">
                                    <Setter Property="Background" Value="{DynamicResource LowStockBrush}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource LowStockAccentBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource LowStockTextBrush}"/>
                                </DataTrigger>
                                <!-- Sufficient (green) -->
                                <DataTrigger Binding="{Binding Status}" Value="Sufficient">
                                    <Setter Property="Background" Value="{DynamicResource SufficientBrush}"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource SufficientAccentBrush}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SufficientTextBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="★" Width="40" IsReadOnly="True" SortMemberPath="IsEssential">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="⭐" FontSize="14" HorizontalAlignment="Center"
                                               Visibility="{Binding IsEssential, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               ToolTip="Essential Item"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="⚠" Width="56" IsReadOnly="True" SortMemberPath="IsNonEssentialIn990Bin">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource DangerBrush}" CornerRadius="8" Padding="6" HorizontalAlignment="Center"
                                            Visibility="{Binding IsNonEssentialIn990Bin, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            ToolTip="Non-essential item in 9-90 bin - should not be here!">
                                        <TextBlock Text="⚠" FontSize="14" Foreground="{DynamicResource TextOnPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Item Number" Binding="{Binding ItemNumber}" Width="SizeToCells" MinWidth="90" IsReadOnly="True" SortMemberPath="ItemNumber"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding DisplayDescription}" Width="SizeToCells" MinWidth="140" IsReadOnly="True" SortMemberPath="DisplayDescription"/>
                        <DataGridTextColumn Header="Tags" Binding="{Binding TagsDisplay}" Width="SizeToCells" MinWidth="60" IsReadOnly="True" SortMemberPath="TagsDisplay"/>
                        <DataGridTextColumn Header="Bin Code" Binding="{Binding BinCode}" Width="SizeToCells" MinWidth="70" IsReadOnly="True" SortMemberPath="BinCode"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding QuantityOnHand}" Width="SizeToCells" MinWidth="60" IsReadOnly="True" SortMemberPath="QuantityOnHand"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="SizeToCells" MinWidth="80" IsReadOnly="True" SortMemberPath="StatusSortOrder"/>
                        <DataGridTemplateColumn Header="Dict" Width="50" IsReadOnly="True" SortMemberPath="DictionaryMatched">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="✓" FontSize="14" Foreground="{DynamicResource SuccessLightBrush}" HorizontalAlignment="Center"
                                               Visibility="{Binding DictionaryMatched, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               ToolTip="Matched in Dictionary"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Status Bar -->
                <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource SurfaceHoverBrush}" BorderThickness="0,1,0,0" Padding="16,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" FontSize="12"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" 
                                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="⏳" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Loading..." Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>





