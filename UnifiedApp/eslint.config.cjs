/** Minimal flat ESLint config (v9+). */
module.exports = [
  {
    languageOptions: {
      ecmaVersion: 2021,
      sourceType: 'module'
    },
    rules: {
      // Keep default rules empty for now; the project can opt-in to stricter rules later.
    }
  }
];
